using ContactList.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;

namespace ContactList.Controllers
{
    public class BusinessRecommendationsController : ApiController
    {
       /* [HttpGet]
        public IEnumerable<BusinessRecommendations> Get()
        {
            return new BusinessRecommendations[]{
                new BusinessRecommendations {  CustomerId = 1 , Details = "microsoft", LeadId = 2 , Product = "sharepoint"},
                new BusinessRecommendations { CustomerId = 2 , Details = "microsoft", LeadId = 4 , Product = "BizTalk"}
                   };
        }

        // POST api/values
        public bool Post([FromBody]BusinessRecommendations value)
        {
            return true;
        }  */
    }
}
